import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import Header from "@/components/header"
import Footer from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "AL NIZAM MECHANICAL ELECTRICAL WORK CONTRACTING LLC",
  description:
    "A MEP licensed government-authorized and fully registered company providing top-quality mechanical and electrical contracting services in UAE.",
  keywords: "MEP contractor, electrical work, mechanical contractor, Dubai, UAE, contracting services",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}

